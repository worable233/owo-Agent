#include <iostream>
#include <filesystem>
#include <cstdlib>
#include <string>
#include <vector>
#include "single_include/nlohmann/json.hpp"
#include "include/curl/curl.h"
#include "app.h"
                                            
// 简化命名空间
using json = nlohmann::json;
// 声明函数
int set_model();

// 入口点
int app::run(int argc, char* argv[]) {
	

	return 0;
}
// 设置模型
int set_model(std::string id) {
	if () {
		
	}
}